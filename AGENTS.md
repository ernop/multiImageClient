# MultiImageClient - Agent Entry Point

Start here. Read this file and the linked documents before doing anything.

## What This Is

C# desktop app that chains together image generation steps across multiple APIs (BFL/Flux, Ideogram, Recraft, OpenAI gpt-image-*). Supports prompt composition, randomization, Claude rewrites, permutations. Includes an experimental Django gallery. (DALL·E 3 support was removed after OpenAI retired the model from the API on 2026-05-12; gpt-image-* is the replacement family.)

## Also Read

- [.cursorrules](.cursorrules) — communication style, XML docs policy, namespace rules, constants philosophy
- [.cursor/rules/writing-register-ste100.mdc](.cursor/rules/writing-register-ste100.mdc) — **mandatory writing register for all agent output**: no praise; Simplified Technical English ASD-STE100; max 20 words per sentence; one fact per sentence; active voice; instructions start with a verb; no undefined jargon; DataColada/Gelman rigor over textbook consensus; 3Blue1Brown/Deutsch standard for math and natural science; Scott Alexander epistemics in succinct prose
- [.cursor/rules/production-deploy-target.mdc](.cursor/rules/production-deploy-target.mdc) — **mandatory release identity**: this repo deploys only the private MultiImageClient production site; `tpbeta` is its shared physical host, never the product target
- [.cursor/rules/shared-site-resident-memory.mdc](.cursor/rules/shared-site-resident-memory.mdc) — **the private production `--ui` site is a resident daemon**: disk is source of truth, RAM/caches are bounded, and full image bytes never remain resident for process lifetime. Say **"shared-site resident"** or **"production UI memory"** to force this + clear the listed debt.
- [docs/shared-host-memory-budget.md](docs/shared-host-memory-budget.md) — the private MultiImageClient site's RAM budget on physical host `tpbeta` and its reasoning framework (no-swap 4 GiB box, layered retention→intake→Chromium→self-heal fixes with commit references), plus per-tenant RAM surveys. Current since 2026-08-05: MemoryHigh=2048M/MemoryMax=2560M/GC 50% after tpdiscord and tpbeta.uwsgi were retired and the app became the box's primary tenant.
- [.cursor/rules/atomic-preloading-viewers.mdc](.cursor/rules/atomic-preloading-viewers.mdc) — **mandatory for every quick/preloading viewer**: never expose a previous item's media or metadata while a new selection loads; clear hidden presentation state and swap media + chrome atomically.
- [docs/fail-closed-policy.md](docs/fail-closed-policy.md) — mandatory no-fallback policy: preserve exact identity, fix upstream, and fail hard on ambiguity or incomplete output
- [docs/ideation-mode-prd.md](docs/ideation-mode-prd.md) — **core top-level product need (not yet implemented)**: Claude-powered Ideation Mode for the `--ui` web app. User briefs Claude (text and/or pasted images), Claude returns N highly-variable structured concept proposals via forced tool use (guaranteed-parseable JSON), user curates idea cards individually or auto-fans-out all N ideas × M generators. Keep this in mind when touching the UI; hone toward it.
- [docs/grok-web-video-browser-transport.md](docs/grok-web-video-browser-transport.md) — grok-web edit/video integrity transport: pure-C# Statsig signing in production, with Playwright retained only for signing-material capture and explicit CLI fallback
- [docs/provider-onboarding.md](docs/provider-onboarding.md) — researched 2026-07-30: signup steps, endpoints, pricing, and prompt-cap quirks for candidate new providers (Seedream, Reve, Luma, Qwen, MiniMax, Runway; Firefly = enterprise-gated skip; Midjourney = hosted-shim or midjourney-web routes only).
- [docs/b2-image-hosting-plan.md](docs/b2-image-hosting-plan.md) — **Backblaze B2 image hosting** (provider switched from Bunny 2026-08-05 on running cost + counterparty; full staged plan + all decisions settled same day). Primary goal is STORAGE offload (production disk 91% full, eternal retention), not serving relief. Decided: fully-open `allPublic` bucket with opaque-random-key capability URLs; upload failure = retry ×3 then visible hard-fail, NEVER serve the local copy as substitute; per-install retention via `B2KeepLocalRawImages` (dev keeps raws, production evicts after verified upload — B2 becomes raw-byte SoT there); v1 uploads results+grids only. Owner checklist, curl smoke test, settings keys, and the verified 2026 running-cost comparison in the doc.

## Production Release Identity

The sole production product target for this repository is the owner's private
shared image-making site at
`https://multiimageclient.alpha.fuseki.net/<private-path>/`. The path segment
is secret and must never be printed or committed. Changing the hostname, path,
TLS, or nginx is a separate migration, never part of an ordinary release.
`tpbeta` is only the colocated machine hosting it. A request to “push and
deploy” means push the requested Git branch, then redeploy only
`multiimageclient-ui.service` using [deploy/README.md](deploy/README.md).
Never deploy or restart a neighboring site, vhost, or service.

Live production limits verified 2026-08-06 are 1 contact-sheet finalizer,
14 aggregate provider requests, default 64 pending jobs, scheduler-default
lane caps, 3 GiB free-disk reserve, `MemoryHigh=2048M`, and
`MemoryMax=2560M`. The tracked MultiImageClient named vhost returns 404 for
unknown paths; it does not install a default-server 444 catch-all. Any
machine-level default vhost is outside this repository. These verified limits
supersede older production figures later in this file.

## Related Projects

- **SocialAI** (`/proj/SocialAI/`) — Discord bot for Midjourney image capture
- **ideogramHistoryDownloader** (GitHub) — absorbed into `tools/ideogram-export/`; see `docs/ideogram-history-archive.md`
- **cmdline-dalle3-csharp** (GitHub) — DALL-E 3 CLI (predecessor, may be subsumed here)
- **IdeogramApiCSharp** (GitHub) — Ideogram API client (predecessor)
- **myBrowser** (`/proj/myBrowser/`) — meta-project; see `capabilities.md` for cross-project skill inventory

---

# Repository Guidelines

## Project Structure & Module Organization
`MultiImageClient/` hosts the C# console orchestrator; `Program.cs` wires runs. `Workflows/` handles execution pipelines (`BatchWorkflow`, `RoundTripWorkflow`, `GeneratorGroups`); `ImageGenerators/` holds one adapter per provider — BFL, Ideogram v2 + v3, GPT-Image-1, GPT-Image-2, Recraft, Google Gemini image, Google Imagen 4 (DALL·E 3 removed 2026-07 after the 2026-05-12 API shutdown); `Describers/` implements image→text (Claude, OpenAI, Gemini, local InternVL, local Qwen); `promptGenerators/` produces prompt sources; `promptTransformation/` rewrites text (Claude rewrite, randomizer, stylizer); `Utils/` supplies helpers. Shared contracts and `Settings.cs` live in `ImageGenerationClasses/`. Provider-specific low-level clients sit in `BFLApi/`, `IdeogramAPI/`, and `RecraftAPI/`. `djangoManager/` contains the experimental Django gallery; it hasn't been touched in a year and is not actively developed. `tools/` holds standalone Python utilities, notably `tools/vid2img/` — the video→context→gpt-image-2 workflow (yt-dlp download, ffmpeg frames + scene cuts, timestamped contact sheets, faster-whisper transcript, iterative `gen` against `/v1/images/edits`; one module per pipeline step, see its README). `do_flask_intern.py` is an optional local InternVL3 Flask server; `save_b64.py` decodes base64 responses. Generated artifacts collect in `saves/` and `output*.png` — ignore them in commits.

## Build, Test, and Development Commands
All projects target plain `net10.0` (retargeted from `net9.0` on 2026-08-05; `MultiImageClient.Web` is `net10.0-windows`) and are cross-platform — the app runs fine on Linux (compositing uses ImageSharp/Magick.NET, not WinForms; verified in production use on a Linux box, 2026-08-03). Verify the SDK with `dotnet --list-sdks`; if 10.x is missing, `winget install Microsoft.DotNet.SDK.10` on Windows or `dotnet-install.sh --channel 10.0` on Linux (fuseki runs it from `tparkour`'s `~/.dotnet`). Restore with `dotnet restore MultiImageClient.sln`. Compile with `dotnet build MultiImageClient.sln`. Execute runs with `dotnet run --project MultiImageClient/MultiImageClient.csproj`; prompts come from `prompts.txt` and `settings.json` (the latter must be created by copying the template `settings - Fill this in and rename it.json`). On current `master` the build is clean (0 errors); ~90 warnings are all `NU190x` advisories for `Magick.NET-Q16-AnyCPU 14.8.2` — safe to bump to `14.12.0` when convenient. For the Django tooling, create a venv in `djangoManager/`, install `requirements.txt`, and launch `python djangoManager/imageMaker/manage.py runserver`. Run `dotnet format MultiImageClient.sln` before opening a PR.

## Run Modes (CLI flags)
See `RunOptions.cs` for the source of truth; this is the current surface:

**Grok naming rule (user-facing surface):** there are two ways this app can hit Grok image/video generation, and every flag/name distinguishes them: **`grok-api`** = the official `api.x.ai` API-key version (public, GDPR-suitable content ruleset) — flags `--grok-api-showcase`, `--grok-api-pro`, `--grok-api-edit`, `--grok-api-edit-aspect-ratio`, `--grok-api-video-test`, `--grok-api-sync`, `--grok-api-export`, REPL gens `grok-api` / `grok-api-pro`. **`grok-web`** = the consumer `grok.com` cookie-session version, i.e. "me using the web app", with the American web-app content ruleset — flags `--grok-web`, `--grok-web-pro`/`--grok-web-quality` (Pro tier, the DEFAULT), `--grok-web-fast` (non-pro tier), `--grok-web-cookies`, `--grok-web-mode`, etc. Internal C# type names keep the historical `Grok*` (API) / `GrokWeb*` (web) prefixes; only the addressable surface uses grok-api/grok-web.

**Meta naming rule (user-facing surface):** Meta's Muse Image has **no official public API** as of 2026-07 (only the consumer Meta AI app + a separate Muse Spark text API). So the only programmatic path is **`meta-web`** = the consumer `meta.ai` browser-session version, mirroring `grok-web` — flags `--meta-web`, `--meta-web-headed`, `--meta-web-cookies`, one-time `--playwright-install`; `--showcase --gens meta-web`. Internal C# types use the `MetaWeb*` prefix; reserve `meta-api` for a future official API. It is **unofficial and best-effort**: Meta retired the HTTP persisted-query GraphQL generation path in favor of an integrity-checked WebSocket transport ("DGW") whose messages only the real browser can produce, so `MetaWebClient.cs` drives the actual meta.ai web app with **Playwright** (persistent Chromium profile at `Settings.MetaWebBrowserProfilePath`, default `~/.config/multi-image-client/meta-ai-profile`): type the prompt into the real composer, let Meta's own JS speak DGW, wait for new (post-submission) CDN images to settle, download them through the browser context, validate content-type + magic bytes, save the bytes verbatim. Auth: log in once with `--meta-web --meta-web-headed` (profile persists the session), or inject a cookie export via `Settings.MetaWebCookiePath` / `--meta-web-cookies` — needs HttpOnly `datr` + `ecto_1_sess` (`abra_sess` optional), full jar preferred. Deliberately unclaimed on this transport: orientation control, exact image count (Meta decides), image upload/editing. Diagnostics: `MetaWebCaptureSessions` writes events.jsonl + failure screenshots under `saves/<day>/meta-web-capture/` (never cookie values). Three-layer split (mirrors grok-web): `MetaWebCookieLoader.cs` (cookie parse/validate), `MetaWebClient.cs` (Playwright browser transport, one shared instance per workflow/server, internally serialized), `ImageGenerators/MetaWebImagineGenerator.cs` (`IImageGenerator` adapter). Entry point `Workflows/MetaWebWorkflow.cs`. Not yet exposed in `--ui` (do that only after a live smoke test passes). Caveats: may break when Meta changes page/selectors/anti-automation; region/account eligibility applies; cost shows `0m` because there is no metered price, not because it's free; images may carry Meta provenance/watermarks.
- *(no args)* — interactive: pick Batch (1) or RoundTrip (2), y/n/edit each prompt from `PromptFiles`.
- `--auto` — skip menu (defaults to Batch), auto-accept every prompt.
- `--workflow 1|2` — 1=Batch, 2=RoundTrip.
- `--limit N` — stop after N prompts.
- `--prompt "..."` — single inline prompt via `InlinePromptSource` instead of `PromptFiles`.
- `--input-image /path/to/image.png` — run RoundTrip once from this image file (implies workflow 2 if no workflow was specified). Without it, RoundTrip prompts interactively for an image path.
- `--fast` — one fixed gpt-image-2 low/1024x1024/moderation=low call per prompt. Cheap smoke-test config.
- `--open-images` — master switch for popping finished images/contact-sheets open in the system default viewer. **OFF by default** — runs are headless and just save to disk. Every viewer launch in the app funnels through `ImageCombiner.OpenImageWithDefaultApplication`, gated by `ImageCombiner.ViewerPopupsEnabled` (set from this flag in `Program.Main`). The showcase one-shots (`--grok-api-showcase`, `--all-providers`, `--provider-sample-showcase`) build/save their grids regardless but only pop them open when this is set.
- `--showcase [--gens csv]` — **generic contact-sheet one-shot**: run ALL prompts from the active prompt source (honoring `--limit`; no default cap) through the selected generators, one contact sheet per generator. `--gens` picks generators by short name (`gpt2`, `grok-api`, `grok-api-pro`, `grok-web`, `meta-web`, `ideogram`/`ideogram-v4`, `ideogram-v3`, `ideogram-v2`, `recraft`, `bfl`, `google`, `googlepro`, `local-klein`, `local-zimage`); without it the standard batch set (`GeneratorGroups.GetAll`) runs. `grok-web` honors the `--grok-web-*` flags (cookies, aspect ratio, pro/fast tier) and `meta-web` honors the `--meta-web-*` flags (cookies, headed), both at concurrency 1. Keyless generators are skipped with a log line; unknown names abort. Implementation in `Workflows/ShowcaseWorkflow.cs`; the name→generator catalog is `GeneratorGroups.BuildByShortName`.
- `--quick-test` — like `--fast`, plus every streamed partial PNG is saved AND popped in the default viewer (implies `--open-images`, since live viewing is the point). Still asks y/n per prompt unless combined with `--auto`.
- `--backfill-dl` — one-shot: mirror every image under `Settings.ImageDownloadBaseFolder` into `Settings.FlatImageMirrorPath` and exit.
- `--b2-smoke` — one-shot: verify Backblaze B2 image hosting config end to end (upload random bytes, anonymous fetch through the public bucket URL, byte-compare, random-wrong-key 404 check, delete) and exit. Requires `EnableB2ImageHosting=true` + the `B2*` settings.
- `--b2-backfill` / `--b2-backfill-dry-run` — one-shot Stage 5 migration (2026-08-10, `Implementation/B2BackfillWorkflow.cs`): upload pre-hosting UiHistory raster results/grids to B2 (SHA-256 re-verified before upload; hidden prompts/images and inputs excluded), rewrite each job's persisted `events.jsonl` to the B2 URLs + legacy `thumbs[]` (exact-URL replacement, atomic, `events.jsonl.pre-b2` kept), and evict local raws only when `B2KeepLocalRawImages=false` and a closing re-read proves no local full-res reference remains. Run with the UI server STOPPED. Dev raws are never deleted (owner requirement). **Scope widened 2026-08-11 (owner decision):** videos (MP4), streamed progression snapshots, and failure-kept previews are hosted too — live path and backfill — with `progressImages[].url`/`partialImages[]` rewritten and local `thumb`/`partialThumbs` card previews added; SVG and inputs stay local. Snapshot/kept-preview upload failures keep the local URL (documented exception: the artifact stays local-served and unevicted; a successful generation must not fail over a preview nicety), while video uploads follow the results policy (retry ×3 then visible hard-fail). UI-mode gpt-image-2 no longer writes day-folder `PartialsLive` copies (the durable per-job snapshots replaced them), and video-source reuse (`Make Grok video` from an evicted result) refetches the exact recorded B2 object with SHA-256 verification via `UiJobRunner.TryGetImageBytesIncludingHostedAsync`. **Thumb expiry (2026-08-11, owner decision):** disk card thumbs older than 2 days are deleted by a 6-hourly `UiThumbExpiry` sweep, but ONLY when their source is still obtainable — local original on disk, or (hosting on) a recorded `CdnKey`+SHA-256; irreplaceable thumbs (pre-B2 images whose raws are gone forever) are never deleted since they're the last remaining visual for their cards. A request for a missing thumb rebuilds it on demand: from the local original as before, or via `UiJobRunner.TryRebuildCardPreviewFromHostedAsync` (SHA-verified streamed B2 download to a temp file, single-flight per image, 3 concurrent regens max). Rebuild refreshes mtime, so actively viewed archive days stay warm. **B2 hosting (implemented 2026-08-05, default off, awaiting owner's B2 signup for live verification):** when enabled, finished `--ui` result/grid images upload to a public B2 bucket under opaque random capability keys (`ui/{jobId}/{gen}/{n}-{128-bit hex}.{ext}`), `gen-result.images[]`/`grid.url` carry absolute B2 URLs plus an index-aligned local `thumbs[]` array for cards, upload failure = retry ×3 then visible hard-fail (never a local-URL substitute), and `B2KeepLocalRawImages=false` (production) deletes checksum-verified local raws after finalization with thumbs force-built first. Uploads set `b2-content-disposition: inline; filename="<local save's descriptive filename>"` (2026-08-10) so browser save-as gets the sanitized prompt+generator name instead of the opaque capability key; `--b2-smoke` verifies the header is served back. Videos/SVG/inputs/partials stay local in v1. The bucket needs a share-with-every-origin CORS rule (the viewer preloads via `fetch()`). See [docs/b2-image-hosting-plan.md](docs/b2-image-hosting-plan.md).
- **Production UI browser policy (updated 2026-08-06):** Chromium-backed features remain disabled in `--ui`. `grok-web` text-to-image uses the browser-free imagine WebSocket. Grok-web **image edit and video are browser-free** when the exact current `GrokWebStatsigVerificationKey` + `GrokWebStatsigAnimationKey` pair is configured: `GrokWebStatsigSigner` signs direct app-chat HTTP for both payloads because each uses the same complete request identity, `POST /rest/app-chat/conversations/new`. The UI reports grok-web image-capable/video-capable only with a complete validated pair. Capture/refresh the public deployment pair via one explicit CLI operation, `--grok-web-capture-statsig --input-image <path>`: Playwright invokes the real Edit control, intercepts and aborts before generation, and the C# signer must reproduce the captured header byte-for-byte before settings are written. Image edit was live-verified locally 2026-08-05 (direct .NET HTTP accepted; one edit returned a 1328x784 image in ~50s); browser-free video's first provider test is pending. Missing/malformed/stale material and provider rejection fail closed; never guess. `meta-web` remains absent from UI. Explicit CLI modes retain Playwright only as a fallback when signing material is absent.
- `--ui [--ui-port N]` — **local web UI** (the project's first GUI; default port 5960, binds 127.0.0.1 only). Kestrel + minimal APIs hosted inside the console binary (`FrameworkReference Microsoft.AspNetCore.App`, no NuGet additions), serving a no-build static frontend from `MultiImageClient/Ui/wwwroot/` (source-tree copy preferred at runtime, so editing `app.js` + refreshing the browser works without a rebuild). Core gesture: paste an image from the clipboard (Ctrl+V anywhere, or drag-drop/browse), type a prompt, pick generators, watch results fill in live side-by-side. The complete current endpoint catalog is exposed: `gpt2`, `gpt1`, `gpt1-mini`, `ideogram` (V4, text-only), `ideogram-v3` (image-capable; split from V4 on 2026-07-28), `recraft`, `bfl`, `google`, `googlepro`, `grok-web`, `grok-api`, `grok-api-pro`, `meta-web`, `local-klein`, and `local-zimage`. Unconfigured targets remain visible but disabled with their exact availability problem; the two local ComfyUI targets (`local-klein`, `local-zimage`) are additionally gated behind `EnableLocalGenerators` in settings.json (default **false** = "not installed": disabled in every UI, skipped by showcase/batch runs, never installed automatically — the gate lives in `ProviderKeyValidator.DescribeKeyProblem`, which every surface consults); Enable all / Disable all / Toggle all act on every available target, and while an image is attached two more bulk buttons appear: "Enable image-capable" and "Disable text-only". **Attachment policy (user-specified, 2026-07-28): text-to-image-only endpoints stay selectable on image jobs and run from the prompt alone — the attached image is simply not sent to them.** The UI labels this behavior everywhere: every chooser chip carries a tiny picture icon when the target accepts an image (a slashed red one appears on text-only targets while an image is attached), and each such result cell gets a red "text-only" badge stating the attachment was not sent. `gpt2`, `grok-web`, and both `grok-api` tiers auto-route to edit generators when an image is attached (the image-capable set is defined once as `UiJobRunner.ImageCapableKeys`, exposed per-generator as `imageCapable` in `/api/config`, and consumed by the frontend — never hardcode it in `app.js`); `recraft` runs Recraft's `/images/imageToImage` (strength default 0.1 — that scale is VERY aggressive: 0.2+ already discards the source composition entirely, observed 2026-07-20; input contract is <10 MB / <16 MP / max dimension <4096 px / min 256 px, violations get rejected as `image_too_big`, so `RecraftGenerator.ConformImageToImageInput` deterministically downscales+re-encodes oversized inputs before upload and logs the transformation — undersized inputs are a hard error); `google`/`googlepro`/`bfl`/`ideogram-v3` use the pasted image as a style/reference guide (Ideogram V3 accepts style references; the `ideogram` V4 target is JSON-only and therefore text-only — the two are separate targets since 2026-07-28). Remaining targets run image-input jobs from the prompt alone per the attachment policy above. Recraft V4.1 has NO style/substyle/artistic_level/negative_prompt surface (all V2/V3-era concepts, rejected or ignored on V4.x; API-created custom styles are V3-only) — but it does honor `n` (1-6), `random_seed`, and `size` as either exact `WxH` or aspect `w:h` (omitting size lets Recraft auto-pick from the prompt); `RecraftGenerator` exposes all of these (`sizeOverride`/`imageCount`/`randomSeed`/`imageStrength` ctor params) and the UI maps shape→aspect + passes n. **Option-mapping policy (2026-07-20):** if a provider endpoint offers a knob, the generator + UI support it; if a selected target doesn't support a configured option (n, shape, detail, quality...), the job still runs and the option is silently ignored — the UI only hard-disables a target for capability gaps (currently: image-input on text-only targets). Per-target mapping lives in `UiShapeMapping` (`Implementation/UiJobs.cs`): gpt-image-2 exact WxH; gpt-image-1 3 canonical sizes + n≤10; grok-api AR + 1k/2k + n≤10; Recraft aspect + n≤6 (+seed in ctor); Ideogram V4 native 2K resolution by shape + num_images≤8 (+seed; detail is a no-op, v4 is 2K-native — and as of 2026-07-20 the v4 endpoint silently IGNORES num_images: HTTP 200, one image back, so GetCost stays per-call; V3 honors it), Ideogram V3 (own `ideogram-v3` target: style-reference path with an image, plain text-to-image without one, auto = square) AR enum + num_images≤8 (verified working); BFL explicit WxH (~1 MP standard / ~4 MP high+max, multiples of 32, no n on the API); Google AR + imageSize 1K/2K/4K (no n — Gemini returns what it returns). Multi-image URL responses (Recraft/Ideogram/grok-api with n>1) are downloaded in-generator and returned as `Base64ImageDatas` since `ImageManager`'s Url path only saves one file. grok-web has NO arbitrary n: `enable_side_by_side` false=1 image, true=4 (the web transport's only counts). Architecture: jobs are the unit — `POST /api/jobs` (multipart: prompt + generators + options + optional image) returns an id; `GET /api/events/poll?cursor=N` is a cursor-based poll over one global envelope log (`job-known` metadata announcement per job, then its `accepted`/`gen-start`/`gen-result`/`grid`/`job-done` events; cursor=0 replays the full history, out-of-range cursors resync from 0). **Transport rule: the UI holds ZERO persistent connections** — plain-HTTP localhost gets no HTTP/2, so every long-lived SSE/WebSocket permanently occupies one of the ~6 HTTP/1.1 sockets browsers allow per origin ACROSS ALL TABS; per-job EventSources and later even one-stream-per-window both starved all `<img>` loads and made pages go blind (observed twice, 2026-07-27). The frontend polls every 1s (5s when the tab is hidden), and the logs panel likewise uses `GET /api/logs/poll?after=seq` instead of SSE. Result bytes are served from memory at `/api/jobs/{id}/images/{gen}/{n}`; `?thumb=1` returns a <=640px card preview (JPEG q80, PNG if the source has alpha, memory-cached per job) and cacheability is per IMAGE, not per job (fixed 2026-08-05): any path-backed response — durable original, disk thumb, input, video — gets `Cache-Control: private, max-age=31536000, immutable` because a durable path is recorded exactly once, when that gen/index's final bytes are saved; only memory-backed streaming partials stay `no-store` (their stable URLs advance from partials to finals). Keying on job.IsDone made users reviewing each other's fresh multi-generator jobs re-download every already-final multi-MB original per viewer open until the last generator landed. **Card-image rule (2026-07-28):** job cards and the input-library picker always display `?thumb=1` previews with an explicit `aspect-ratio` placeholder from the event's size, while anchors/viewer/video-source/set-active keep the plain full-resolution URL — a 200+ job history otherwise pulls gigabytes of originals through the browser's ~6-socket HTTP/1.1 pool (collapsed height-less cells defeat `loading="lazy"`, and same-origin tabs' API calls starve behind the image queue; observed 2026-07-28 as "images not loading" plus a second tab stuck without even `/api/config`). Each successful `gen-result` carries a `cost` field (the generator's `GetCost()` estimate — per-image ceiling x n, 0 on failure/free targets); the frontend shows it per result cell, per job, and in a session `#cost-summary` bar with a per-generator breakdown (collapsible to just the headline total via a show/hide toggle whose state persists in localStorage), all recomputed idempotently from cell `data-*` attributes so SSE replays/reconnects never double-count. Everything still saves to disk through the standard `ImageManager` pipeline + combined contact sheet — and when the job carried an input image, the combined sheet's FIRST cell is that input image, gold-framed and labeled `INPUT` in black with a per-generator statement of exactly what function it served ("edit source", "image-to-image source", "style/reference image", "video source", or "NOT sent (text-only target, prompt only)"; role text built by `UiJobRunner.BuildInputImageRoleText`, rendered by `ImageCombiner.CreateBatchLayoutImageSquareAsync`'s `inputImagePath`/`inputImageRole` params, which fail closed if a path arrives without a role, 2026-08-02); uploaded input images are archived under `saves/<day>/UiInputs/` — **with the saved extension + content type matching the actual bytes**: the upload is format-sniffed (ImageSharp `Image.Identify`), PNG/JPEG/WEBP saved verbatim under their true type, and any other decodable format (GIF/BMP/... from a web drag-drop) deterministically re-encoded to PNG before the job starts (logged pre-operation input conformance; undecodable uploads are a hard 400). This matters because providers sniff the bytes — Ideogram rejects any style reference that isn't PNG/JPEG/WEBP, and the old code force-labeled unknown formats `image/png` without converting (`IdeogramV3Generator` also hardcoded `image/png` on the multipart part; it now detects the real type from magic bytes and hard-errors on unsupported formats — fixed 2026-07-28 after live "image was not provided in one of supported formats" rejections). Implementation: `Workflows/UiWorkflow.cs` (server), `Implementation/UiJobs.cs` (job model + runner, modeled on `ReplWorkflow.ProcessOneAsync`). Jobs live on the server for the life of the process; every browser window/tab is a view — `GET /api/jobs` + the replayable SSE stream let a (re)loaded page hydrate all history, while composer state (draft prompt/attached image) stays per-window. Output geometry is intent-level, no freetext sizes: a **shape** picker (auto / square 1:1 / landscape 3:2 / portrait 2:3 / wide 16:9 / tall 9:16) plus a **detail** tier (standard ≈1K / high ≈2K / max ≈4K-ish), mapped per generator by `UiShapeMapping` in `Implementation/UiJobs.cs` — gpt-image-2 gets an exact WxH from a validated table (all multiples of 16, inside the pixel envelope; square max is 2880x2880 = exactly the 8 294 400-pixel cap), grok gets an AR string + 1k/2k resolution, and edits with shape=auto inherit the source image's shape. grok-web edit-with-image now works by riding the SAME imagine WebSocket as text-to-image (`wss://grok.com/ws/imagine/listen`): `GrokWebImagineEditGenerator` uploads the source via `/http/upload-file-v2/direct`, creates a media post, then runs browser-backed `POST /rest/app-chat/conversations/new` with `modelName: "imagine-image-edit"` and `mediaGenInput.imageToImage.inputAssets: [assetId]`. Playwright opens the post and clicks grok.com's real **Edit** control so `x-statsig-id` is attached (plain fetch / HttpClient still 403). The older `properties.image_uri` imagine-WebSocket path is intentionally unused for edits: the transport accepts the field but ignores the source and invents a new scene from the prompt alone (observed 2026-07-31). The upload response nests the asset id as `fileMetadata.fileMetadataId` (top-level `uploadId` is NOT an asset id — `GrokWebClient.TryExtractAssetId` handles this). With shape=auto the edit derives the nearest supported AR from the source file's pixel dimensions so it keeps the input's shape.
- **UI Ideogram 4.0 image input (2026-08-09; supersedes older text-only/JSON/`num_images` claims above):** `ideogram` is image-capable. Text jobs use multipart `POST /v1/ideogram-v4/generate`; attached-image jobs send the primary image through multipart `POST /v1/ideogram-v4/remix` with `text_prompt`, output `resolution`, and `rendering_speed`. Remix accepts one PNG/JPEG/WEBP up to 10 MB; oversized supported inputs are deterministically downscaled and re-encoded to PNG before the provider call. The UI omits optional `image_weight`, allowing Ideogram to choose source influence from the edit instruction. Auto output shape maps the source to the nearest published 2K resolution; an explicit shape wins. The current API has no V4 `num_images` or `seed` field, so UI `n` performs separate single-image requests. Current V4 rejects `FLASH`; UI uses `DEFAULT`. Published per-image Generate/Remix estimates are Turbo $0.03, Default $0.06, and Quality $0.10.
- **UI grok-web details:** **prompt cap (2026-07-30):** the consumer imagine WebSocket hard-rejects prompts over **8192 chars** with "Prompt is too long (invalid_parameter)" (instant, pre-generation, free; verified empirically 8192 OK / 8193 rejected — the official grok-api 4096 limit does NOT apply to this transport). The constant is `GrokWebClient.MaxPromptChars`; `/api/config` surfaces it per generator as `maxPromptChars` and the composer shows a amber non-blocking `#prompt-limit-notice` whenever the prompt exceeds a selected target's cap. Submission stays allowed: `GrokWebClient.GenerateImageAsync` truncates over-limit prompts at the send stage (explicit user-required product behavior, logged, never splits a surrogate pair) — this covers both grok-web text-to-image and edit since both ride the same method; other selected targets still receive the full prompt. Separately: the official grok-api supports prompt-aware `aspect_ratio: "auto"` and the UI sends it. The consumer grok-web imagine WebSocket does not: live tests on 2026-07-20 showed both literal `"auto"` and omitted `aspect_ratio` returning 768x1152 (2:3) for strongly panoramic and square prompts, while explicit `16:9` correctly returned 1280x720. Therefore (2026-07-28) grok-web UI auto requests an explicit square `1:1` — a declared pre-call input default, chosen because Grok's native 2:3 is a poor universal shape and no prompt-aware auto exists on this transport; explicit shapes map as usual. Edit-with-image auto continues to derive/inherit the source dimensions. Side-by-side mode is on by default and asks for up to four images (`n` does not control grok-web; `--grok-web-no-side-by-side` asks for one). Every successful image tile from any provider has **Make Grok video** as a light opaque "grok video" chip floating over the tile's top-right corner, visible only while hovering that result (absolute-positioned: zero reserved space, instant snap, keyboard-focusable): a follow-up dialog with an optional motion prompt, required Normal/Funny/Custom/Spicy method, 1–15s duration (default 10s), and 480p/720p resolution (default 480p). It posts `/api/video-jobs`, reuses the selected persisted result bytes as the source upload, runs `GrokWebImagineVideoGenerator` with side-by-side off, streams job progress through the normal SSE machinery, saves the MP4 under `saves/<day>/Video/`, and displays it in the custom video player. Consumer method values sent to Grok are `normal`, `fun`, `custom`, and `extremely-spicy-or-crazy`. Live browser research on 2026-07-20 confirmed that empty-prompt image-to-video succeeds when the method suffix is present, and that video still uses `/rest/app-chat/conversations/new`; this endpoint returns 403 to standalone HTTP but succeeds from a real logged-in browser. Therefore only the video app-chat POST runs through a shared Playwright page (`GrokWebBrowserClient`); image/edit remain on the imagine WebSocket and upload/post/poll/download remain HTTP. One-time setup is `--playwright-install` unless `GrokWebBrowserExecutablePath` points at Chrome/Chromium. See [docs/grok-web-video-browser-transport.md](docs/grok-web-video-browser-transport.md).
- **UI Recraft variants (2026-08-04):** the chooser exposes `recraft` (V4.1 raster), `recraft-v41-utility`, `recraft-v41-pro`, `recraft-v41-vector`, `recraft-v3`, `recraft-v4`, and `recraft-v4-pro`. All accept image-to-image input and n up to 6; image jobs inherit source dimensions and reject explicit output AR overrides. Vector raw output is preserved as SVG while cards and contact sheets use raster previews. Published per-image estimates are V4.1/Utility $0.035, V4.1 Pro $0.21, V4.1 Vector $0.08, V3/V4 $0.04, and V4 Pro $0.25.
- **UI describe endpoints (image → text, 2026-08-05):** the chooser has a second, separately-labeled **describe** section whose targets return TEXT descriptions of the attached image(s) instead of images: `describe-ideogram` (Ideogram `/describe`), `describe-openai` (gpt-4.1 vision), `describe-claude` (claude-sonnet-4-5), `describe-gemini` (gemini-2.5-pro), `describe-grok` (grok-4.3 via api.x.ai). Placement (user-specified 2026-08-05): the output-params row (AR/detail/quality/n/moderation) stays directly under the media chooser it applies to, and the describe section sits BELOW that row (those options don't apply to describe targets); the whole section is HIDDEN, not disabled, while no image is attached — nothing to describe means no chips at all. The local `describe-local-internvl`/`describe-local-qwen` targets were REMOVED same day: neither local server exists on the dev box or production, so they only produced instant failures (the CLI describer classes remain for batch workflows). Rules: (a) an attached image is REQUIRED — chips disable without one, `POST /api/jobs` hard-rejects, and each selected endpoint describes EVERY attached input separately (one text per input); (b) the composer prompt is the describe instruction when non-blank; a blank describe-ONLY job gets the standard instruction (`UiJobRunner.DefaultDescribeInstruction`, exposed via `/api/config` `describe.defaultInstruction`; the composer substitutes the same text at submit so the card shows exactly what was recorded) while a blank mixed job is still rejected; (c) Ideogram `/describe` takes no instruction — the prompt is NOT sent to it and its result label says so; (d) describe results ride the normal event pipeline as `gen-result` with `resultKind:"text"`, `texts:[{inputIndex,text,comments,raw}]`, `sentPrompt`, `images:[]` — persisted to events.jsonl like everything else, so live cards, reloads, and day-archive cards all render them identically; (e) describe cells are excluded from the combined contact sheet (a describe-only job builds no sheet at all); (f) blank text from any endpoint is a hard per-target failure, never an empty success. **JSON reply contract (2026-08-05, user-specified):** the instruction-capable targets (all but Ideogram) receive wire prompt = user instruction + `UiJobRunner.DescribeJsonReplyContract`, which forces a JSON object `{"description", "comments"}` — description is ONLY the clean description (models kept appending "Would you like me to..." chatter), comments absorbs all meta remarks and renders as a smaller separated block; the contract also asks (user requirement) for complete matter-of-fact coverage of every person's apparent age, sex, and appearance in support of the project's fairness/equality goal, and requires (user requirement) that resemblance to any real-world person be mentioned: "For everyone in the image who resembles a real-world person, you may mention the resemblance — and indeed must." `ParseDescribeJsonReply` is strict fail-closed (markdown fence stripped deterministically; non-JSON or blank description = hard target failure); OpenAI additionally gets native `text.format json_object` and Gemini `responseMimeType application/json` (Claude/Grok transports have no such knob — prompt only). **Gemini gotcha:** gemini-2.5-pro CANNOT disable thinking — `thinkingBudget: 0` is HTTP 400 "Budget 0 is invalid. This model only works in thinking mode." (looks like billing, is request shape); `GeminiVisionDescriber` sends a fixed 1024 budget and adds it to `maxOutputTokens` since thought tokens count against that cap. Frontend: each result renders as a describe block (full-width UNCLAMPED description — no per-result input thumbnail, since the job head right above already shows the input image (user decision 2026-08-05) — + optional "model comments" + copy button + "view with image" link + a collapsed **sent / returned** `<details>` exchange viewer showing the exact full wire prompt and the raw pre-parse reply — Ideogram's shows "nothing is sent"); the view link is the viewer-walk anchor (`data-viewer-image` with `data-result-kind="text"`, carrying the viewer-seen mark), and in the JS viewer the stage shows the SUBMITTED input image while a dedicated `#image-viewer-describe` panel above the prompt carries the description + comments ("<gen> — description of the submitted image" header; `c` compare stays armed but paints nothing since input IS the stage). Scheduling: describe targets ride their provider-account lanes (openai/xai-api/google/ideogram) plus a new `anthropic` lane. Cost estimates per call: Ideogram $0.01 (published), OpenAI ~$0.015, Claude/Grok ~$0.025, Gemini ~$0.025 (includes mandatory thinking tokens); the session bar counts them as "results". Implementations: `Describers/RemoteVisionDescribers.cs` (OpenAI/Claude/Gemini/Ideogram clients + `DescriberImageFormat.DetectMime` magic-byte MIME detection — Claude/Gemini/Grok data URIs must carry the true type, not hardcoded PNG), existing `GrokVisionDescriber`; catalog + dispatch in `UiJobs.cs` (`DescribeKeys`, `BuildDescriber`, `RunDescribeOneAsync`).
- **UI composition sketch + layout map (2026-08-11):** two composition tools, one per direction. (a) **Sketch composer**: a `sketch a composition` button under the paste zone opens a modal canvas (`#sketch-dialog`) — square/landscape/portrait/wide/tall canvas drawn with the mouse, a default 8-color legend (red/blue/green/orange/purple/cyan/yellow/brown) each with a "what this color means" field, plus eraser, brush size, undo, clear, clipboard/file paste onto the canvas, crop/rotate/flip, and palette dither/recolor (the MS Paint-class set is the editor minimum; see Sketch Composer Editor). **Regen template from image colors** replaces that stock legend with 1–8 colors taken from the current canvas (blank meaning fields) and snaps the pixels onto those exact hexes so attach's exact-pixel scan succeeds; the labels you then write go into the composition guide. Attach exports the canvas PNG as a NORMAL composer input image and upserts one marked paragraph (marker `Composition sketch legend:`) into the prompt instructing every image model to follow the sketch's placement/coverage but repaint completely in the prompt's style, never rendering the sketch's colors, outlines, or label text. Only colors actually drawn enter the legend (exact-pixel palette scan at attach time; unlabeled used colors get a confirm). Re-attaching replaces the previous sketch attachment by exact File object identity and rewrites the marked paragraph — never stacks copies or guesses among attachments. Canvas work remains session-only and does not enter portable personal configuration, but every submitted sketch job persists strict per-job editor identity in its `accepted.sketchComposer` event (v1: `version` + exact ordered `inputIndex` + aspect + 8 meanings for the built-in palette; v2: plus an explicit 1–8 `{name,hex}` palette with matching meanings; new submits are v2). **Set active is atomic and fail-closed:** concurrent restores use abort/version identity; every input and the identified sketch PNG must fetch and decode completely with the recorded dimensions before composer attachments, prompt, or hidden editor state swap. The hidden editor is cleared before every replacement. A job whose older event lacks this identity never guesses from prompt text, attachment position, filename, or appearance: it keeps the restored attachments, opens a blank editor, and explains that the legacy sketch cannot safely be selected. Viewer shortcuts stay inert while the dialog is open. **Sketch-capable model set (owner live-validated 2026-08-11):** only some targets actually FOLLOW a sketch's layout — `UiJobRunner.SketchCapableKeys` = gpt2, grok-web, grok-web-chat, grok-api, grok-api-pro, bfl (FLUX.2 Pro Preview), bfl-flux2-pro, bfl-flux2-max, bfl-flux2-flex, google (Nano Banana 2); notably EXCLUDED after testing: Nano Banana Pro, Ideogram 4.0 Remix, all Recraft, Kontext, Klein, FLUX1.1/1, Krea, and every text-only target. Exposed per-generator as `sketchCapable` in `/api/config` — never hardcode the set in `app.js`. The sketch dialog has a default-ON, session-only checkbox ("also deselect models that can't follow sketches"): attaching then deselects every checked non-capable media target (describe/analysis selections untouched). While the exact current sketch attachment is present (including an identity-verified set-active restoration), non-capable media chips turn amber (`.gen-toggle.sketch-unsupported`, stronger when checked) with a tooltip stating the model won't follow the sketch — re-enabling one is allowed and never blocked, the tint is the standing reminder. Removing the sketch attachment clears the tint. (b) **Layout map** (`layout-map`, label "Layout map (Gemini 2.5 Pro)"): the reverse — an analysis target in the describe section (requires an attached image; output options don't apply) whose result is an IMAGE: gemini-2.5-pro names each attached input's main sections/topics as 0–1000 `[ymin,xmin,ymax,xmax]` boxes under the strict fail-closed `UiJobRunner.LayoutMapJsonContract` (`{summary, regions:[{label, box}]}`, 1–8 regions; `ParseLayoutMapJsonReply` hard-fails non-conforming replies — the only cosmetic tolerances are a markdown fence and numeric coordinate rounding), and `UiLayoutMapRenderer` draws a flat-color section map at the source aspect (largest-first paint, numbered labels with collision-aware placement, numbered legend band + the model's one-sentence summary baked into the PNG; saved under `saves/<day>/LayoutMaps/`). Results ride the normal image gen-result pipeline (cards, viewer, favorites, video follow-ups, B2 upload retry×3 hard-fail) but contribute no contact-sheet cell, like describe. The composer prompt, when present, is passed as label CONTEXT with the same blank-prompt substitution rules as describe. Shares the `google` scheduler lane and the `GoogleGeminiApiKey` gate with describe-gemini (~$0.025/image estimate). `UiJobRunner.IsAnalysisKey` (describe ∪ layout-map) now drives describe-section placement (`kind:"describe"` in `/api/config`), requires-image rejection, blank-prompt substitution, and generator-preset validation on both server and frontend.
- **UI Grok video sources and iteration:** every successful image tile, from any image provider, can be sent to grok-web video generation. Video aspect defaults to the source image, with explicit 1:1 / 3:2 / 2:3 / 16:9 / 9:16 overrides. Every successful video has **Redo Grok video**, which reuses the archived source image (not the MP4) and restores the prior prompt, method, aspect, duration, and resolution for editing, plus **Save video** for downloading the original MP4. Players share one browser-local volume and mute state across every video (initially 50% and unmuted), and start at exact 1:1 pixels. Video jobs do not build image contact sheets, and the frontend suppresses obsolete contact-sheet links when replaying older video-job events.
- **grok-web video integrity transport (updated 2026-08-06; supersedes older browser-only descriptions):** direct unsigned HTTP/fetch still gets 403. The UI now sends video through pure-C# `x-statsig-id` signing with no Chromium, using the same method/path identity as the live-verified browser-free image-edit request. It is available only when the exact validated deployment pair and cookie file are configured; otherwise `/api/video-jobs` fails closed. The previous **Make Video → Quick Animate** Playwright path remains only as a CLI fallback and as historical end-to-end protocol evidence.
- **Per-endpoint extra text + private notes (2026-08-10; supersedes the gpt-image-2-only settings control):** the generator gear's **per endpoint** view exposes `append extra text` and `private notes` for every image endpoint, including hidden or currently unavailable targets. `/api/config` supplies both defaults per endpoint: gpt-image-2 keeps the default anti-murk/daylight suffix; every other endpoint's extra text and all notes start blank. A reset restores the current server default. Authenticated users persist only explicit overrides in `UiCommunityStore`/SQLite; auth-off local mode stores the same validated shape in `localStorage.mic_generator_preferences_v1`. Each nonblank extra-text value is appended after a blank line only to that exact generator's independent prompt copy, recorded as `ManualSuffixation`, persisted in the job's `accepted.generatorExtraTexts`, and shown with that endpoint's image in the viewer. Blank gpt-image-2 extra text is an explicit override and produces a visible warning; pre-feature browser windows retain deterministic compatibility through the old `gpt2Guidance*` form fields and localStorage keys. Notes appear only in the owner's configuration and chooser tooltip: they are included in personal configuration export/import but are never submitted with a job, sent to a provider, emitted in job events/logs, or exposed through another user's config response. Unknown, duplicate, unselected, describe-target, oversized, or over-total extra-text/configuration data fails closed.
- **UI kept last preview on failed streams (2026-08-10, user-required product behavior):** a generation that fails AFTER emitting streaming preview partials — typically gpt-image-2's final image getting `moderation_blocked` while partials already rendered — keeps its last streamed partial visible instead of vanishing it at `gen-result`. Server: `UiJob.TryPersistLastPartialImage` (called from `RunOneAsync` on `!ok`) writes the in-memory partial bytes to `UiHistory/{id}/partials/` via `UiJobStorage.PersistEphemeralImage`, re-registering the SAME stable `{gen}/{n}` image key as path-backed — so the live preview URL, `?thumb=1` card preview, replayed `gen-partial` events, and restarts all keep serving that last known state (immutable-cacheable: the URL's bytes never change again after failure). The failed `gen-result` stays a failure — `ok:false`, zero cost, error text intact — and carries the kept URLs in a separate index-aligned `partialImages` field; the frontend renders each under the error text with a red `partial-kept-badge` reading "last preview before failure — not the final image", deliberately NOT a viewer-walk anchor and excluded from favorites/video flows. Kept previews stay local-served even with B2 hosting on (failure artifacts are not uploaded; same v1 scope as videos/SVG) and are never eviction candidates. No partial received (failure before the first partial, or N>1 where streaming is off) means no `partialImages` — nothing is invented.
- **UI in-process previews kept on success too (2026-08-10, user-required product behavior):** the streamed gpt-image-2 progression is revisitable on SUCCESSFUL generations as well, not only when the final fails. Server: each arriving partial is durably persisted the moment the callback fires via `UiJob.TryPersistPartialSnapshot` under its own snapshot key `{gen}~p{partialIndex}/{n}` (verbatim bytes into `UiHistory/{id}/partials/` through the same `PersistEphemeralImage`; bytes never retained in RAM beyond the stable-key live preview that already exists), so the final overwriting the stable `{gen}/{n}` key no longer destroys the in-process frames. `gen-result` carries them — success AND failure — in an arrival-ordered `progressImages` field (`{partialIndex, imageIndex, url}`); the unchanged failure-only `partialImages`/kept-last behavior sits on top, so a failed cell shows the kept-last tile plus the full progression. Snapshot keys never enter `images`/contact sheets/favorites/video flows, are never B2-uploaded or eviction candidates (local-served even with hosting on, same v1 scope), are immutable-cacheable path-backed responses, and share the hide identity of the result image they preview (`UiJob.PartialSnapshotVisibilityGen` maps `gpt2~p*` → `gpt2` in the image route, so hiding an image hides its snapshots). Frontend: `buildProgressPreviews` renders a collapsed `<details class="progress-previews">` strip ("in-process previews (N)") under the cell's results in both branches — thumbs (`?thumb=1`) load only on first expand so a long history fetches nothing extra; tiles are plain new-tab links, deliberately NOT viewer-walk anchors. A snapshot whose disk write failed is simply absent from the strip, never substituted.
- **UI input-image aspect default (2026-07-26):** the untouched `auto` output-AR option changes its visible label to **match input image** whenever an image is attached. The server validates and records source dimensions, then resolves that default for every image-capable endpoint: source-ratio WxH for gpt-image-2 and BFL; nearest native ratio for grok-web/grok-api, Ideogram V3, and Google; Recraft `/images/imageToImage` inherently follows the source dimensions and exposes no size override. Any explicit shape selected by the user wins wherever the provider endpoint has an AR/size control; Recraft disables for image jobs with an explicit AR because silently ignoring it would be false success. Text-to-image `auto` behavior is unchanged. Invalid shape values or undecodable image uploads fail before a job starts; missing dimensions never fall back to square.
- **UI naming rule + composer polish (2026-07-28):** result cells always show the SAME display name as the generator chooser (the `/api/config` catalog `label`, e.g. "Nano Banana 2" — never internal spec strings like "google ui"); the provider's spec label travels in `gen-result.label` and renders only as the cell-head tooltip, while the actual returned pixel size travels separately as `gen-result.size` and renders in a small `.cell-size` span beside the name (events persisted before this date embed the size in `label`; the frontend regex-extracts it on replay). The contact-sheet link shows no on-disk path (path is the link tooltip). The AR picker right-aligns the numeric ratio column (server sends `text`/`ratio` per shape; frontend NBSP-pads in a monospace select). The options "?" is a click-toggled popover (was hover-title only). **Fix spelling / undo fix** buttons directly under the prompt (`#prompt-tools`): `POST /api/prompt/spellfix` runs the prompt through `ClaudeService.FixSpellingAsync` (temperature 0, system prompt forbids rephrasing — spelling-only; browser spellcheck suggestions are not scriptable, which is why this is server-side), gated on `AnthropicApiKey` via `ProviderKeyValidator.DescribeTextKeyProblem` and surfaced as `spellfix` in `/api/config`; undo restores the exact pre-fix text held client-side (a Haiku spellfix call costs a small fraction of a cent — ~$0.0002 for a typical prompt at $0.25/$1.25 per MTok). **McPhee (updated 2026-08-13):** current McPhee 3.10.0 is vendored under `MultiImageClient/Ui/wwwroot/mcphee/` from canonical `C:\proj\mcphee`; scripts, the global, and CSS classes use McPhee names, with typo-js/Hunspell dictionaries and the word-frequency list vendored. Its overlay is verified on `#prompt`; the standard profile checks spelling, spacing, culture/proper-name usage, and repetition, including exact repeated multi-word phrases. A collapsed-by-default **spelling** panel in the prompt area provides suggestions, add-to-dictionary, ignore, formality, and rule controls. **fix typos (local)** uses undo-preserving `applyFixes`; the old personal-dictionary storage key `mic_spellwell_custom_dict` is intentionally retained for exact continuity. Explicit `.aff`/`.dic` static MIME mappings remain in `UiWorkflow`. Every job card's prompt has a two-overlapping-squares **copy** icon (outside `.job-prompt`, whose textContent is read verbatim by the video dialog and image viewer) that copies the exact prompt and flashes "prompt copied". Job cards/cells are deliberately tight: 4-6px radii, 8px gaps. **Input library:** "load a previous image" under the paste zone lists every user-uploaded input image via `GET /api/input-images` — user uploads only (video-job source copies excluded via `SourceJobId`), deduplicated by SHA-256 of the archived bytes (hash cache per job id), newest first, each entry carrying its original prompt/dimensions/date as the tile tooltip; picking one re-attaches those bytes to the composer. Unreadable archived bytes are omitted and logged, never guessed at.
- **UI viewer input comparison (2026-07-29):** in the image viewer, `c` toggles a GLOBAL sticky "always compare with the input image" mode (persisted in `localStorage.imageViewerCompareInput`): while on, any viewed image whose job carried an input image splits the stage into two labeled halves — archived input first (`/api/jobs/{id}/images/input/0`), generated output second — and jobs without an input show the normal single view with the mode still armed. **Compare orientation (2026-08-16):** the two panes are not always side by side — `imageViewerCompareOrientation` computes both side-by-side and stacked (input on top) for the available box and applies whichever draws the images larger (`.stacked` on `#image-viewer-stage.compare`; ties keep side-by-side), so tall pairs sit beside each other and wide pairs stack; `fitImageViewerWindow` compares candidates by drawn per-image height (`imageHeight`, half the stage in stacked mode), and user-pinned windows re-decide from the actual stage box on resize/input-load. `c` again restores the user's normal view everywhere. Wired through `applyImageViewerCompare` + the `compareInput` entry in `ImageViewerCommands` (so the `?` shortcut list and the `?` button tooltip both document it); job cards expose `dataset.hasInputImage` for the viewer's prompt walk. Same day (revised 2026-07-30): the viewer window **shrink-wraps to its content** — `fitImageViewerWindow` sizes the window so the image (or both compare panes) draws at the largest undistorted size the screen allows and no larger, so a portrait image on an ultrawide gets a narrow window while a wide image or compare mode can take the full width (never full-screen "just for fun"; the old 1400x950 cap is gone). It runs after each image decodes, when the compare input's aspect arrives (`load` listener), and on browser resize; two passes because the status bar's wrap height depends on window width. First-ever open pre-sizes to the viewport while loading; later opens keep the previous size until the new image decodes (no full-screen flash). A manual drag-resize takes precedence until reload (`dataset.userSized`), and a 16px margin always keeps click-outside-to-close reachable. Stage images use `width/height:100%` + `object-fit:contain` so they draw at the largest undistorted size including upscaling small originals. Added 2026-08-05: the info panel (generator, resolution, prompt, guidance) is no longer always a bottom bar — `fitImageViewerWindow` computes both candidate layouts and moves the panel to a fixed-width 280px right column (`.side-status` on `#image-viewer-window`, width constant `ImageViewerSideStatusWidth` in `app.js` must match the CSS) whenever that draws the image strictly larger; ties keep the bottom bar. Wide images and compare mode (doubled stage width makes height the binding constraint) typically go side; portrait/square images typically keep the bottom bar. In side mode the whole column scrolls as one and the prompt/guidance height caps are lifted. Added 2026-08-05: `Shift+F` toggles browser fullscreen on the viewer overlay (`toggleImageViewerFullscreen`; entering clears `dataset.userSized` so the window re-shrink-wraps to the whole monitor via the resize event both fullscreen transitions fire); while fullscreen, Esc only exits fullscreen (viewer stays open, second Esc closes), and `closeImageViewer` exits fullscreen itself so click-outside-to-close never leaves a black screen.
- **UI viewer close handback + seen marks (2026-08-05):** (a) **Close handback** ("inner movement implies outer movement"): optional, OFF by default — when ON, closing the viewer (Esc or click outside) scrolls the page to the image that was on screen (only if its thumbnail isn't already fully visible; `block:center`) and focuses its anchor instead of restoring pre-open focus, so arrow-key travel inside the viewer hands its final position back to the page. Persisted per-browser as `localStorage.imageViewerReturnSync`; toggled by the settings-panel checkbox (`#viewer-return-sync`) or instantly with `s` inside the viewer (the `?` shortcut list documents it with a live ON/OFF readout — `help` entries in `ImageViewerCommands` may be functions; toggling `s` while the list is open refreshes it in place instead of dismissing it). The outer element is resolved by exact identity (`findViewerAnchor`: job id + generator + image index against `a[data-viewer-image]` datasets), never by position; a missing anchor (filtered/hidden card) means no corresponding place, so the close proceeds without scrolling. (b) **Seen marks**: every image actually painted full-size in the viewer is recorded per-browser (`localStorage.mic_viewer_seen_v1`, `jobId|generator|imageIndex` keys, capped at 5000 with oldest-first eviction) and its card thumbnail gets a subtle accent edge (`.viewer-seen` on the anchor, applied both at paint time and at `gen-result` cell render on replay) — unofficial "have I zoomed this?" bookkeeping. (c) **Departure pulse**: on close, the just-viewed image's thumbnail pulses a brief accent glow (`.viewer-return-pulse`, 3 cycles) so the eye lands on it — in BOTH handback modes. Note the viewer's keyboard walk re-reads the DOM on every navigation (`getImageViewerPrompts`) and relocates the current position by identity, so jobs submitted/finished while the viewer is open become reachable with Left/Up (new cards prepend, newest at top) without shifting the image being viewed. (d) **Cold-open thumbnail preview** (added 2026-08-05 after "first image takes forever" on remote deployments): opening the viewer previously showed an empty stage until the ENTIRE multi-MB original fetched+decoded. Now `paintImageViewerColdOpenPreview` immediately paints the clicked item's OWN card thumbnail (already decoded on the page) at stage size together with its chrome, with the dimensions slot reading "loading full resolution…", then the decoded full-res frame swaps in — atomic-viewer compliant because everything shown identifies the pending target. Applies ONLY when the stage is empty (cold open; the `!imageViewerImage.getAttribute("src")` check), never during warm navigation, which keeps the previous coherent frame; skipped when the clicked anchor's lazy thumb hasn't loaded (`img.complete`/`naturalWidth` guard); the "seen" mark and compare split still wait for the real frame. The window also shrink-wraps to the thumb's aspect ratio immediately, so no geometry jump on swap. (e) **Hover prefetch** (2026-08-05): resting the pointer on a result thumbnail for 150ms fetches the full-resolution original so the click paints from the browser HTTP cache (finals are served immutable — see the per-image cache rule). Single-flight, latest hover wins; the intent timer cancels on pointer-out but a started transfer completes; disabled while the viewer is open (it runs its own prioritized preload window); `data-viewer-image` anchors are images only, so videos are never prefetched. Prefetch failures are silent by design — speculative cache warming; the click path's own fetch surfaces real errors.
- **UI shared favorites (2026-08-06):** the persistent favorite system has explicit `image` and `prompt` resource kinds. In the generated-image viewer, `v` (or the adjacent star button) adds/removes the current "creating as" user's image favorite; `f` toggles fullscreen (Shift is not required, and `Shift+F` also resolves to fullscreen). Each job card also has a `☆ prompt` control beside its prompt, which favorites the entire submitted prompt by exact originating job ID. Image identity remains the exact `(jobId, generator, imageIndex, user)` edge; prompt identity is `(jobId, user)` — no text deduplication, positional lookup, or fuzzy correlation. Gold means your own favorite and blue/shared counts mean other users. The bar below the people filters opens everyone's or one named user's mixed image/prompt gallery, including live and archived jobs without hydrating whole archive days. `GET /api/favorites` returns both grouped resource collections and `POST /api/favorites` idempotently sets a requested `kind` + boolean state. `UiFavoriteStore` persists one atomically-written JSON file per user/resource edge under `ImageDownloadBaseFolder/UiFavorites`; disk is source of truth and the in-process dictionary is a lightweight index. Adding first validates image identity against one successful persisted `gen-result`, or prompt identity against the exact persisted job; removing remains possible if old job files later disappear. Describe/text and video results cannot be image-favorited, but their originating whole prompt can be favorited. Browser windows refresh shared state every 5s (30s while hidden), and an unchanged snapshot does not rebuild the gallery DOM.
- **UI floating activity center (2026-08-07):** a subtle bottom-corner bar reports the current browser user's queued/running/completed generator status plus shared return/favorite activity. A creator-return notice is emitted on their first accepted image/video job ever or after six hours without another accepted job. Favorite-add notices preserve exact actor, target job, generator, and image index; removals do not notify. The panel is draggable/resizable and stores expansion, geometry, per-category switches, sound (default OFF), and browser popup notifications (default ON but still subject to browser permission) in `mic_ui_settings_v1`. Repeated generator updates condense in place and browser notifications share one replacement tag. The expanded panel lets any authenticated site member submit a developer request; text is capped at 4000 characters and stored in SQLite, and only authenticated `ernieMultiZone` receives the request inbox/API. `UiCommunityStore` uses `UiCommunityDbPath` or `ImageDownloadBaseFolder/ui-community.sqlite3`; activity is capped at 5000 rows, requests remain durable, and reads page bounded results without resident history. Transport remains short polling (`/api/activity/poll`, `/api/requests`) with zero persistent connections.
- **UI account profile attribution (2026-08-08):** authenticated users can explicitly apply the current "creating as" name to all creations securely linked by `UiJob.CreatorLogin`. This changes presentation and people-filter grouping retroactively without rewriting the original `UiJob.CreatedBy` in `job.json`; jobs without `CreatorLogin` remain under their historical name until an administrator assigns ownership outside this flow. Profiles and private alias reservations live in `UiCommunityStore`; every historical name linked to the account and every auth login name is reserved case-insensitively so another account cannot claim it. `GET /api/profiles` exposes only opaque public owner IDs and current display names, and clients poll its revision to update open live/archive cards. `POST /api/profile` is authenticated and user-initiated; after a profile exists, the server ignores browser-submitted attribution and records future jobs under the profile's current display name. Developer-only `POST /api/admin/legacy-attribution` assigns exactly one legacy job by `jobId` + exact `expectedCreatedBy` to an existing auth `targetLogin`; it refuses jobs that already have an owner and preserves `CreatedBy`.
- **UI job reuse + viewer restyle (2026-07-28):** Multi-image results (any `n>1`, grok-web's side-by-side 4) render as a two-column grid inside the cell (`.cell-images.multi`) instead of a full-width vertical stack — four half-width images at equal AR stack to exactly one full-width image's height, so the grok-web card matches the single-image generators. Every non-video job card has **set active** in its meta column: it copies the job's entire setup back into the composer — archived input image (refetched from `/api/jobs/{id}/images/input/0`), prompt, generator selection, and the options recorded on the job's persisted `accepted` event, which now carries `detail`/`quality`/`moderation`/`n` alongside `shape`; jobs persisted before the options were recorded restore prompt/image/generators only (recorded fields are applied, missing ones never guessed). The generated-image viewer was restyled: navigation has no onscreen directional buttons and closing is Esc or a click outside the window (close button removed); the stage background is dark (`--ink`) so unused area recedes; the old white-on-blue monospace status bar is gone, replaced by a bottom bar in the main page's visual language (card background, ink text, system-ui) showing exactly the generator display name, the returned resolution, and the full wrapping prompt (scrolls past ~5 lines). **Viewer traversal update (2026-08-06):** a compact 1-based **Place-in-list indicator** (`current / total`) is again always visible and is painted atomically with the selected media; **Intent-filtered wheel stepping** normalizes pixel/line/page wheel units, ignores horizontal intent, and advances only after an accumulated threshold; mouse Back/Forward thumb buttons perform **Mouse side-button stepping** without changing browser history; PageUp/PageDown retain five-item **Fixed-distance jumps**; Ctrl+PageUp/Ctrl+PageDown add corrected endpoint-reaching **Halfway jumps** (repeated presses converge through the remaining distance), while Ctrl+Left/Ctrl+Right remain prompt-boundary jumps. The `?` overlay is a named control glossary: each interaction has a product-level name plus its exact behavior.
- **UI shared-site layer (2026-08-03):** the web UI is deployable as a multi-user shared site. (a) **Day archive**: `UiJobRegistry` restores all of `saves/UiHistory/` but only TODAY's jobs ride the live envelope feed; older jobs group by day behind `GET /api/archive/days` (list) + `GET /api/archive/days/{day}` (that day's jobs + full event history, same JSON as the live feed), rendered lazily by the frontend's `#archive` section through the identical `addJobCard`/`applyJobEvent` path — copy-prompt, set-active, viewer, video follow-ups all work on archived cards. (b) **Usernames**: every job POST requires a `user` (1-32 chars, letters/digits/space/._-; stored as `UiJob.CreatedBy`, persisted in job.json, carried in job-known envelopes and `/api/jobs`); the composer's "creating as" control persists in localStorage (`mic_username`); `GET /api/users` feeds multi-select person-filter chips in `#people-bar` (empty selection = everyone; persisted as `mic_user_filter_v1`). Deliberately attribution-only — no privacy, everyone sees everything. (c) **Access gate**: optional `Settings.UiAuthFilePath` → `ui-auth.json` `{enabled, secret(≥16 chars), accounts:[{username,password}]}` (`Implementation/UiAuth.cs`); login sets a ~10-year HttpOnly cookie `mic_auth = username.HMACSHA256(secret, username\npassword)` validated per-request against the CURRENT file (stat-gated 1s reload), so deleting an account or changing its password/secret revokes instantly; broken-after-startup file fails closed (enforced, zero valid tokens); configured-but-invalid file at startup aborts the server; per-IP throttle 10 failures/5 min; unauthenticated page loads get an inline login page, API calls get 401 JSON and the frontend reloads into the login page. Blank `UiAuthFilePath` = fully open, local behavior unchanged. (d) **Reverse-proxy prefix**: ALL frontend URLs (fetches, image srcs, and server-generated event URLs persisted as `/api/...`) resolve through `apiUrl()` against the page directory, so nginx can serve the app under a secret path with a plain prefix-stripping `proxy_pass` — no Kestrel path-base config. Deployment kit in `deploy/` (nginx default-deny 444 catch-alls + secret path + login rate limit, hardened systemd unit, `ui-auth.example.json`, README). Threat model in [docs/shared-site-security.md](docs/shared-site-security.md).
- **Shared-host resource controls (updated 2026-08-04):** UI endpoint work uses a process-wide fair `UiTargetScheduler`: jobs split into provider/account lanes (`openai`, `xai-api`, separate grok-web WS/browser lanes, `meta-web`, `google`, `bfl`, `ideogram`, `recraft`, `comfyui`), ready lanes rotate globally, and each lane rotates among users while preserving each user's FIFO order. `UiMaxConcurrentGenerators` (default 20 for local use) is the aggregate open-request cap; `UiTargetConcurrency` supplies validated per-lane caps. `UiMaxConcurrentJobs` now gates only memory-heavy contact-sheet finalization and cleanup, so production's value of 1 no longer blocks unrelated endpoint requests from later jobs. `UiMaxPendingJobs` (default 64) bounds accepted running/queued jobs; overflow returns HTTP 503 instead of creating unbounded `Task.Run` waiters. `/api/status` exposes pending jobs plus per-lane queued/running/limit counts. `UiMinimumFreeDiskBytes` (default 0/off) rejects new image and video jobs with HTTP 503 before reading uploads when the output filesystem is below the configured reserve, and fails closed if capacity cannot be measured. Production on colocated 4 GiB `tpbeta` currently uses 1 finalizer / 20 aggregate requests / 64 pending jobs / 3 GiB reserve, with low per-lane limits and systemd memory/CPU/I/O limits. A dedicated `UiLivenessGuard` OS thread probes the content-free `/healthz` route through a raw loopback socket every 30 seconds after startup; three consecutive failures terminate with `FailFast`, allowing systemd `Restart=on-failure` to recover a process that remains alive but cannot serve HTTP. `MULTIIMAGECLIENT_SETTINGS` selects an exact settings file for split `/opt` code, `/etc` secrets, `/var/lib` data deployments; a declared missing path remains a hard error. The nginx template is a dedicated named vhost and never claims `default_server`, preserving every colocated host.
- `--meta-web [--meta-web-headed] [--meta-web-cookies fp]` — batch prompts from the active source through the `meta.ai` consumer web app (Muse Image) by driving a real Playwright browser session, one contact sheet for the run, concurrency 1. First-time setup is `--playwright-install` (downloads Playwright's Chromium; skip if `MetaWebBrowserExecutablePath` points at an existing Chrome/Chromium), then `--meta-web --meta-web-headed` once to log in. See the Meta naming rule above for transport details and caveats. Implementation in `Workflows/MetaWebWorkflow.cs`.
- `--repl` — **interactive prompt-by-prompt REPL with async dispatch**. Defaults: gpt-image-2 at 2048x2048 / high / moderation=low / n=1, up to 5 prompts in flight concurrently. Each line is either a prompt (fired asynchronously, stdin stays responsive) or a `:command`. Grids are built and saved but NOT opened in the viewer. Commands: `:size WxH`, `:quality low|medium|high`, `:moderation auto|low`, `:n N` (images per gpt-image-2 call; >10 requires confirmation), `:concurrency N`, `:gens list|add|remove|reset` (names: gpt2, grok-api, grok-api-pro, ideogram, recraft, bfl, google, googlepro, imagen4 — imagen4 dead after 2026-06-24), `:status`, `:wait`, `:last`, `:retry`, `:edit`, `:help`, `:quit`. Per-prompt override syntax: `[size=1024x1024,q=low,n=4] a red apple on a white plate`. Initial defaults can be pre-set from the command line via `--repl-size`, `--repl-quality`, `--repl-moderation`, `--repl-concurrency`, `--repl-n`. Implementation in `Workflows/ReplWorkflow.cs`.

- **UI viewer composer activation (2026-08-06):** every image presentation in the viewer carries two compact controls immediately below its prompt. **set image active** replaces all composer attachments with the exact full-resolution viewed image while preserving the current composer prompt; **set image + prompt active** performs the same exact-image replacement and also copies that viewer item's prompt. Neither action changes generator selection or output options. Fetches preserve exact viewer identity, a newer activation supersedes an older in-flight one, and the buttons remain part of the item-specific chrome that swaps atomically with the visible image.

- **UI viewer-only image actions (updated 2026-08-06; supersedes the older hover-chip description above):** **Hide image** and **Make Grok video** never render on main job or favorites thumbnails. Both appear only in the generated-image viewer for the exact image currently displayed, subject to creator permission and video availability. Prompt-level hiding remains on the job card, and **Redo Grok video** remains attached to an existing video result.

- **Per-user generator chooser (updated 2026-08-12):** the small gear beside Enable all / Disable all / Toggle all opens the only editor for target visibility, selected-on-load defaults, whole image/describe section visibility, and up to 20 named personal selection buttons. The editor separates these concerns into **shown**, **defaults**, and **groups** views; each uses the same compact endpoint-tile picker, and groups are created or edited one at a time. Hidden targets disappear from the composer and cannot be selected by a personal button until restored in the shown view. A personal button replaces the complete image-generator selection; it never adds to the current selection. Authenticated users persist preferences by login in `UiCommunityStore`/SQLite across browsers; auth-off local use persists the same validated shape under `localStorage.mic_generator_preferences_v1`. Unknown, hidden-selected, duplicate, or describe-in-image-preset keys fail closed. Three immutable standard selection groups — **Thinking ones**, **can do famous people**, and **text ok** — are defined in `/api/config` independently of personal persistence, so every user in every environment receives them. Group membership is advertised on each endpoint definition as `standardGroupIds`; the frontend reconstructs each group from those exact memberships and hard-fails malformed catalog data. An identical legacy personal group is suppressed only as a duplicate main-row button and remains editable/exportable until its owner deletes it.
- **Portable personal configuration completeness (updated 2026-08-10):** the settings panel exposes one versioned JSON document for the complete portable personal configuration: creating-as name, people filters, generator visibility/defaults/groups and per-endpoint text/notes, UI/activity settings, prompt tools, McPhee data, inspiration library, viewer behavior, video audio, and cost display. `Ui/wwwroot/personal-config.js` owns the format/version, exact-field enforcement, build/normalize pipeline, and single browser storage key; `personalConfigurationFields()` in `app.js` is the canonical field registry with each field's persistence scope, reader, and normalizer. Version 2 removes the duplicate gpt-image-2 guidance field: per-endpoint configuration is its only representation, and the explicit version-1 migrator handles old exports. McPhee's established keys are compatibility mirrors for the independent vendored component, not configuration sources. Credentials, cookies, provider keys, jobs, favorites, and viewed-image history are deliberately excluded. Any future durable user-controlled option—local or account-persisted—must enter the field registry, canonical document, persistence path, coverage note, and schema round-trip/rejection tests in the same change. Unknown versions and missing/unknown fields fail closed; migration must be explicit and identity-preserving.
- **Get Claude's advice (2026-08-08; supersedes the older Fix spelling / fix typos subsection above):** the McPhee spelling overlay and its folded suggestions/settings panel remain, but the one-click local auto-fix button and Claude spelling-only route are removed. **get Claude's advice** opens an instruction editor around the exact current prompt. The server force-appends that source prompt through `ClaudeService.BuildPromptAdviceWirePrompt`; Claude must return only a complete replacement prompt, and undo restores the exact pre-call text. Every exchange is inserted before the provider call and completed afterward in `ui_claude_prompt_exchanges`, retaining identity, timestamps, model, instruction, source, system prompt, wire prompt, raw response, applied result, status, and error. History reads are user-scoped and bounded while the SQLite history remains durable.

- **Contact-sheet producer names (2026-08-11):** every generated-image contact sheet leads each cell with the same complete catalog/model name used by the UI, then the provider (and exact provider model slug where it adds identity), followed by non-duplicate per-call detail. Stable endpoint keys such as `googlepro`, enum names, and UI-only tags are internal identity and never replace the producer name. `GeneratorPresentation` is the canonical registry for UI labels and sheet producer labels; `TaskProcessResult.GeneratorKey` keeps correlation separate from presentation.

### Creator-only stream hiding

Jobs persist `CreatorLogin`, the authenticated account that submitted them, separately from browser-supplied `CreatedBy` attribution. That login and the fixed `ernieMultiZone` override get one-way hide controls. Hiding a prompt removes the whole job from live/archive feeds, favorites, input-library listings, logs, and app image routes. Hiding one image uses exact `(jobId, generator, imageIndex)` identity, redacts only that image, blocks app-route/video reuse, and suppresses its contact sheet while leaving the prompt and sibling results visible. Records live under `ImageDownloadBaseFolder/UiVisibility`; there is no UI unhide. Jobs predating `CreatorLogin` cannot infer ownership from spoofable attribution, so only the override can hide those. This is stream visibility, not byte deletion: already-copied URLs/browser cache entries and underlying local/B2 files are not revoked.

## Coding Style & Naming Conventions
Use 4-space indentation and .NET naming: PascalCase for public types/methods, camelCase for locals, Async suffix for asynchronous methods. Favor explicit types for shared models; use `var` only when the type is obvious. Route new configuration through `ImageGenerationClasses/Settings.cs` instead of ad-hoc JSON parsing. Python utilities under `djangoManager/` should follow PEP 8 snake_case, with comments reserved for non-obvious prompt logic.

## Fail Closed — Never Guess or Substitute
- **Fallbacks are prohibited by default.** Never respond to missing, malformed, ambiguous, rejected, or uncorrelated data by guessing, fuzzy matching, choosing the latest resource, reusing an older result, changing formats, or silently substituting a default.
- Preserve exact identity across every workflow: request id, job id, source generator/index, asset id, post id, hashes, and provider response correlation. If identity cannot be proven, fail loudly.
- Fix the upstream operation, contract, parser, or validation. Do not hide its defect behind downstream recovery.
- Catching an exception must not convert failure into apparent success or continue with substitute data.
- The only exception is a specific fallback explicitly required by the user as product behavior. It must be documented at the call site, independently validated, and incapable of selecting unrelated user/provider data.
- Predeclared defaults selected before an operation begins are ordinary configuration, not recovery. After an operation begins, its failed output must never be replaced with different output.

## Visual & Typography Policy (combined-image output, labels, UI text)
- **Never render text in gray.** No `MutedGray`, no `Color.FromRgb(x,x,x)` where R==G==B in the mid range, no "subtle" gray labels. If a secondary label needs to look secondary, reduce its font size and/or reuse the existing semantic color (e.g. `SuccessGreen`, `ErrorRed`, `Black`, `Gold`) — the contrast comes from size, not desaturation.
- **Controls live next to the input they act on.** A button, toggle, or helper that edits/fixes/clears a specific field sits immediately down/right of that field — not in a distant shared toolbar (e.g. prompt "fix typos" / "fix spelling" under `#prompt`, not beside Generate). Shared submit actions that apply to the whole form stay in the form's send row.
- When reserving vertical space for a text block, include room for descenders (e.g. `g`, `p`, `y`, `j`). `TextMeasurer.MeasureBounds` can under-report; add ~25% of font size as descender padding when stacking text bands.
- Padding above/below a standalone text panel (e.g. prompt panel below the grid) should be proportional to the font size used inside it, not a fixed `Padding * 3`.
- Secondary labels that sit beside a primary label (e.g. per-image timing next to the model name) should be bottom-aligned with the primary label so the smaller text hangs off the baseline of the larger one, not free-floating.

## Universal Image Prompt Defaults
When writing, rewriting, reviewing, or generating prompts for image models, use this default unless the user explicitly requests dark, night, low-key, gloomy, or murky output: clear, bright, full normal daytime lighting; not dim, murky, grimy, muddy, gloomy, shadow-choked, underexposed, dusk-like, night-like, or dark. Prefer readable, coherent, visually organized images with clean composition, clear separation of subjects or groups, concise high-contrast text when text is needed, and attractive balanced color. For OpenAI image models such as `gpt-image-2`, make this explicit in prompt text because the model otherwise tends to drift into dark cinematic drama.

## Image Saving Policy
- **Never resize or re-encode the bytes returned by the image endpoint when saving a Raw variant.** `ImageSaving.SaveImageAsync` must write the API's PNG/JPEG/WEBP bytes verbatim via `File.WriteAllBytesAsync`. Thumbnail-scale downsizing for combined-grid display is fine — that's an in-memory copy used only for layout, never written over the Raw file.

## gpt-image-2 Endpoint Options (reference)
`/v1/images/edits` now has a C# client: `ImageGenerators/GptImage2EditGenerator.cs` (multipart `image[]` files + model/prompt/size/quality/n form fields, non-streaming, ported from `tools/vid2img/vid2img/generate.py`). Input images bind at construction like the other edit generators; do not send `input_fidelity` or `moderation` on `/edits`.

xAI `/v1/images/edits` gotcha: the image input must be `{type:"image_url", url:...}` where `url` may be an HTTPS URL **or a data URI** — the legacy `{type:"base64", data:...}` shape is rejected with "Image input must have either 'url' or 'file_id' set" (observed 2026-07-12; fixed in `GrokImagineEditGenerator.BuildImageInputAsync`).

The `/v1/images/generations` endpoint with `model=gpt-image-2` accepts:
- `size`: `1024x1024`, `1536x1024`, `1024x1536`, `2048x2048`, `2048x1152`, `2560x1440` (QHD — cookbook's "recommended upper reliability boundary"), `3824x2144` (near-4K), or `auto`. Arbitrary resolutions are legal when edges are multiples of 16, max edge STRICTLY less than 3840 (cookbook 1.1), total pixels in [655 360, 8 294 400], and long:short ratio ≤ 3:1. Legacy 3840x2160 is treated as experimental and rejected by `GptImage2Generator.TryNormalizeSize` — use 3824x2144 instead.
- `quality`: `low`, `medium`, `high`, `auto`.
- `moderation`: `auto` (default) or `low` (permissive — we use `low` for batch runs).
- `n`: images per call. We plumb this through `GptImage2Generator.imageCount` (ctor) and surface it in the REPL as `:n N` / `[n=N]` override / `--repl-n` flag. OpenAI only permits streaming when `n=1`, so that case keeps SSE partial previews while `n=2..10` uses the normal non-streaming JSON response. The non-streaming path requires exactly the requested number of indexed array entries with valid `b64_json`; incomplete output fails the whole attempt. Results are returned as separate `CreatedBase64Image` entries so `ImageManager`'s per-index save path produces distinct `...img0`, `...img1`, ... files and the UI displays every completed image.
- `output_format`: `png` (default), `jpeg`, `webp`. `output_compression` (0–100) applies to jpeg/webp. (Not currently exposed in the generator.)
- `background`: `auto`, `transparent`, `opaque` — transparent is not supported by gpt-image-2 (png/webp only, in practice rejected on this model).
- `stream`: `true` only when `n=1`, consuming SSE to surface partials + heartbeat. For `n>1`, omit `stream` and `partial_images` and consume the ordinary JSON `data[]` response. The SSE loop reads UTF-8 bytes through a pooled line reader (`Utf8SseLineReader`) and decodes partials with `GetBytesFromBase64()` directly from the `JsonDocument` — never re-introduce per-event string materialization of the multi-MB base64 payloads; that churn OOM'd the shared host under two concurrent streams (2026-08-04). **Stall watchdog (2026-08-06):** OpenAI streams sometimes die silently mid-run — socket stays ESTAB, partials arrive, the final never does (observed on both installs; one pair of streams sat 15+ hours). `HttpClient.Timeout` only covers `SendAsync`, so post-header body reads had NO timeout, and each zombie held its `openai` scheduler-lane slot forever, queueing every later gpt2 job ("partials spin forever, finals never arrive"). The loop now arms a `StreamStallTimeout` (4 min) that is re-armed only on `data:` lines — non-data lines (`event:` fields, keepalive comments) must never reset it — and a stall throws a visible `TimeoutException` that fails the attempt and frees the lane. Additionally the loop exits successfully the moment all `n` requested `completed` images are in hand: `[DONE]` is just framing and waiting for it held finished results hostage when the server lingered. Partial-save filenames pass through `SanitizeFilename` (a `/` in the prompt used to become a bogus directory component and the write failed).
- `partial_images`: 0–3 (we send 2).
- **Do NOT send `input_fidelity` on `/generations` OR `/edits`** — both endpoints reject it on gpt-image-2 (always high-fidelity). Confirmed on `/v1/images/edits` 2026-07-06 (`invalid_input_fidelity_model`) by `tools/vid2img/`. The OpenAI cookbook's gpt-image-2 edit examples that pass `input_fidelity="high"` are wrong for this model.

Pricing is token-based ($30 / 1M output tokens). Rough per-image ceilings we report: low ≈ $0.02, medium ≈ $0.08, high ≈ $0.25.

## Sketch Composer Editor (later 2026-08-13 update)
This supersedes the earlier sketch-tool and legend-wording details above. The
sketch is described affirmatively to image endpoints as a spatial map:
palette colors identify mapped subjects without prescribing final colors;
outlines, scribbles, and repeated strokes define approximate regions; and
white/unmarked areas are flexible background or negative space.

**Tool-set floor:** this editor is a *basic* composition map, not a full
paint program. Once the MS Paint-class tools are present they are the
**minimum**: paste/load a reference onto the canvas, crop, rotate, flip,
rectangular/free selection with move/duplicate/stretch/rotate, pencil,
brush, eraser, fill, line, shapes, undo/redo, and palette mapping
(recolor, group similar colors, snap/dither onto the 8 legend colors).
Do not ship or regress below that floor. Extra tools may be added; they
are not a substitute for those basics.

While the dialog is open, Ctrl+V / paste / drop / load places the
clipboard or file image onto the sketch canvas (not the composer).
**copy image** / Ctrl+C writes the current canvas pixels back to the
clipboard as `image/png` (the portable clipboard image type; BMP is not
a reliable ClipboardItem write). Paste size is a declared pre-stamp
choice: **match image — resize canvas** (the
default since 2026-08-16: the canvas retargets to the pasted image's own
aspect ratio at its source resolution, upscaled so the long edge is at
least 1024 px and clamped to the 4096-edge / 16 MP caps), keep-aspect
fill (cover), keep-aspect largest fit (contain), or stretch to fill. The
canvas still defaults to square; the shape picker remains the manual
control and shows a disabled `custom w:h` readout while the canvas
aspect matches no published shape. The stamp is an undoable
action and can then be edited with the ordinary tools. Attaching still
requires exact legend-palette pixels, so a pasted photo is mapped with
snap-to-palette, Floyd–Steinberg, ordered Bayer, or group-similar-colors
(plus click recolor / eyedropper) before attach. **Regen template from
image colors** (after paste, with or without a prior stock dither) rebuilds
the working legend from the top non-white colors on the canvas (1–8,
readable unique names, white stays unmarked background), blanks every
meaning field, and nearest-snaps the canvas onto those exact hexes. Undo
restores the previous palette, meanings, and actions together. Canvas
90°/180° rotate and flip keep a published `SketchCanvasSizes` shape when
the canvas is still at a published size (landscape↔portrait, wide↔tall);
after a raw scale they rotate in place at the current pixel size. **Halve**
and **double** are raw nearest-neighbor resizes of the working canvas
(repeatable; live WxH readout in the Canvas group; attach sends whatever
size is showing). Double stops at a 4096-edge / 16 MP cap. Selection crop
keeps canvas size (outside goes white) or scales the selection onto the
canvas with the same paste-size setting; in match-image mode the canvas
takes the selection's own size and aspect ratio.

The editor also has pencil, brush, soft brush, spray, eraser, bounded
tolerance fill, line, rectangle, rounded rectangle, ellipse, triangle,
diamond, pentagon, hexagon, octagon, and star tools, with outline/solid
shape modes and a visible cursor plus active-tool readout. Each committed
operation is an immutable action. A 200-action cap, 32-fill cap, 4096-point
per-stroke cap, and 50-state undo/redo history bound browser work.
Flood-fill traversal buffers are fixed to the current canvas size and
reused. The back-to-front action list can hide/show, remove, or move any
action and rerenders deterministically; clear is itself undoable. A sketch
restored from a persisted job remains identity-safe and appears as one
flattened `restored sketch base` action, because persisted jobs
intentionally retain the exact PNG/aspect/palette/meanings contract rather
than guessing a pre-flattening action graph. Version 1 restores the built-in
8-color palette; version 2 restores the recorded palette. Restore sizes the
editor to the attached PNG's exact pixel dimensions. Attach records the live
canvas aspect (published size, or the nearest published shape by ratio after
a raw scale or match-image paste) so the picker restores; pixel size comes
from the PNG.

## Testing Guidelines
No dedicated test project exists yet. Manually validate new workflows by running representative prompts and inspecting generated assets and metadata. When adding automated coverage, create an xUnit project referenced by the solution and ensure `dotnet test` succeeds. Capture regression prompts in `prompts.txt` with notes after bug fixes.

## Commit & Pull Request Guidelines
Keep commits focused and use imperative, present-tense subjects as in history (`rename and genericize describers`). Include context in the body for prompt sets or configuration changes. Pull requests should outline workflow impacts, note which services (BFL, Ideogram, Recraft) are affected, call out required settings updates, and attach screenshots or sample outputs for UI or prompt adjustments.

## Configuration & Secrets
Copy `MultiImageClient/settings - Fill this in and rename it.json` to `MultiImageClient/settings.json` (already `.gitignore`d), populate only the provider keys for services you intend to use, and never commit secrets. `Settings.Validate()` hard-requires only `LogFilePath` and `ImageDownloadBaseFolder`; every per-generator API key (and the Google Cloud trio: `GoogleCloudLocation`, `GoogleCloudProjectId`, `GoogleServiceAccountKeyPath`) is validated lazily by the generator that actually needs it, so unused generators can be left blank. Optional per-generator keys: `IdeogramApiKey`, `OpenAIApiKey` (GPT-Image-1, GPT-Image-2), `BFLApiKey`, `RecraftApiKey`, `GoogleGeminiApiKey` (NanoBanana), `GoogleCloudApiKey` (Vertex alternative), `AnthropicApiKey` (Claude rewrites & describer). Prompt file list lives in `PromptFiles` (array of absolute paths). `FlatImageMirrorPath` is an optional flat-folder mirror: if set, every saved raw/annotated/combined image is also copied to that single folder (best-effort, never fatal) — leave blank to disable. `TypedPromptsAppendFile` is an optional plain-text corpus: if set, any free-form prompt you type at the interactive batch loop is appended as one line to that file (embedded newlines collapsed, parent folder auto-created, never fatal) — handy for growing something like `2023-prompts.txt` over time. Prefer user secrets or environment variables when scripting automation or sharing runs.

## Structured Generation Archive
Every image/video attempt is archived by default in local SQLite at `{ImageDownloadBaseFolder}/generation-history.sqlite3` (override with `GenerationArchiveDbPath`; disable with `EnableGenerationArchive=false`). The archive records prompts and transformation steps, redacted generator configuration, provider requests/responses and nested fields, success/failure/exception details, timings, costs, and hashes/pointers for files on disk. It never stores API keys, cookies, authorization headers, base64 image bodies, or image/video BLOBs. See [docs/generation-archive.md](docs/generation-archive.md).
